# 按功能分批检查与验收

本轮请求：恢复 particeps-foundation 进度，对未充分验证的代码做深度检查，并按功能分批验收。日期：2026-09-11。

恢复时 Native 处于 Build 修复阶段，第 2 轮候选；既有记录为 81 项中 17 passed、56 failed、8 blocked。这些是上一轮记录，不能当作当前代码已经通过。正式状态和验收报告仍由 Comet Runtime 管理；本文件记录功能批次、代码审查证据和开发期回归检查，不替代正式 Verify。

## 批次与覆盖

每个验收 ID 只分配到一个主批次，共 81 项，无遗漏、无重复。跨模块依赖在各批次检查后统一核对，不删除或降低已有验收标准。

| 批次 | 功能 | 验收 ID | 数量 |
| --- | --- | --- | --- |
| F01 | 认证、凭据与 API | A4、A5、A53、A54、A55 | 5 |
| F02 | 批量任务与恢复 | A8–A15、A43–A46 | 12 |
| F03 | 端口分配与清理 | A16、A17、A29–A33 | 7 |
| F04 | 公网资源与网络模式 | A34、A35、A69–A80 | 14 |
| F05 | CPU、内存、磁盘与带宽 | A18、A19、A47、A48、A58、A61–A63、A81 | 9 |
| F06 | 镜像、登录与实例生命周期 | A6、A27、A36–A41、A59、A60、A66、A67 | 12 |
| F07 | 监控、历史与进程诊断 | A7、A20–A24、A49–A52、A64、A65、A68 | 13 |
| F08 | 部署、环境与集成 | A1–A3、A25、A26、A28、A42、A56、A57 | 9 |

每批依次检查：对应规格 → 实际代码 → 正常与失败路径的自动化检查 → 必需的真实环境证据 → 独立只读复核。只证明部分路径时写明限制，不把编译成功或 HTTP 200 当成功能通过。

## 阶段审阅时的代码与环境基线

以下环境记录保留阶段审阅时的状态。随后配置了 VMnet2 多 IPv4、两个小鸡 IPv6 网段及现有小鸡的静态 IPv6，实际变更和检查见 [本地网络实验环境](../development/network-lab.md)；这是开发环境准备，不改变本文件的代码审阅结论或正式验收计数。

- 用户初始化了本地 Git；本轮已连接 `https://github.com/TXL-325/particeps.git`，在远程原有 `7116571`（仅 LICENSE）之后整理本地提交，保留许可证与历史。
- 测试母机为 Debian 13.6 / Linux 6.12 / VMware，2 vCPU、约 4 GiB 内存、约 50 GB 根盘，Incus 6.0.4，旧 Agent 服务 active/running。
- 仅有一台运行中的 Alpine 系统容器。其真实进程 cgroup 为 `/particeps-guests/lxc.payload.particeps_p-911f4462a5e2`，子组 `cpu.max=50000 100000`、`memory.max=134217728`；父组 `cpu.max=150000 100000`。LVM 逻辑卷大小为 1073741824 字节。以上是配置与内核/卷状态的读取，不是压力验收。
- 实机 IPv4 网桥为 `10.80.0.1/24`、NAT 开启，当前 forward 列表为空；IPv6 网桥关闭、IPv6 转发为 0、没有 IPv6 默认路由。
- 当前代码与 VM 上的旧服务不同。未部署、未重装/删除现存小鸡、未修改母机转发或安全软件设置。

## 分功能审阅结果

下表的“已修复”指代码修改及对应开发期检查，不改变正式验收计数。各批均保留未验证或未完成的项目。

| 批次 | 已实现或本轮已修复 | 仍需完成或验证 |
| --- | --- | --- |
| F01 | bcrypt 管理员密码、会话与 read/manage Token；严格 JSON；会话写入来源校验；凭据 AES-GCM 加密、15 分钟有效、一次性领取；旧明文任务字段迁移清理；前端保留分批领取结果并屏蔽清除后的迟到响应 | 登录限速、审计日志、反向代理下安全 Cookie 策略；Token 撤销按钮；API 文档仍不完整；密码/密钥实际 SSH 登录未验收 |
| F02 | 创建任务及全部子项先事务落库再启动 worker；创建支持幂等键；默认并发 2；任务汇总改为单条聚合 UPDATE，避免终态被旧快照覆盖 | 持久任务执行器、Agent 重启后的任务恢复、失败项重试；批量启停目前仅为浏览器串行调用，失败会中止后续项；创建表单忙碌态未真正接入 |
| F03 | 连续端口选择；实例与 TCP/UDP 预留在一个事务提交；并发预留及中途写入失败回滚已有回归 | 同一 NAT 地址的共享 forward 聚合更新；单端口追加/编辑；宿主与外部 Incus 规则冲突检查；删除转发失败后不得释放端口；DHCP 变更后同步目标地址 |
| F04 | 母机地址候选探测、Web 池设置及网络模式字段；默认 IPv4 NAT 网桥调用 | 地址池合法性与归属检查；实际使用所选 SNAT 地址；独立 IPv4；IPv6 NIC、路由、NAT66；可靠 IPv6 前缀分配及冲突检测。现有字符串拼接算法不能充当 IPv6 分配器 |
| F05 | 0.01 核步长与 CPU 集验证；保留完整设备配置；资源修改先记录意图、保存 Incus operation、确认终态及读回后事务提交；未知结果阻止继续改配/分配；总帽失败恢复且不伪报 Applied；禁止去掉 cgroup 后重试启动 | 多实例同时负载下父/子上限；OOM、磁盘写满与带宽实测；自动核对外部配置漂移；无 operation 引用或已丢失时仍需人工处理，尚无管理恢复界面 |
| F06 | Alpine/Debian 默认模板；普通启停和删除调用；正确检查 Incus 异步结果与命令退出码；密码经 stdin、SSH 公钥错误向上传播 | 关闭密码登录尚未配置 sshd；本地镜像登记未验证实际镜像，创建固定远程镜像源；重装路径未完成；Web 终端未实现；autostart 与 desired_power 协调；销毁/回滚状态机 |
| F07 | 后台采样与浏览器读取分离；CPU/网络独立质量；缺测网络保留 NULL；真实采样间隔；历史配额曲线和断点；轮询取消、页面切换身份校验；进程 CPU 字段如实标为累计秒 | 分钟汇总与 30 天保留；周期流量/清零；完整网络图、概览与排序；进程实时 CPU；采样停止与优雅关闭；无 `-race` 结果，不宣称竞态检测通过 |
| F08 | Linux amd64 构建、嵌入 Web；实验安装脚本；Go 工具链固定 1.26.6；修复 Git 忽略入口源码；补充本地敏感数据排除与 LF 属性 | 安装资源归属/网段重叠检查；升级备份、校验、回滚；守护进程超时与退出管理；8–16 台验证、公网/IPv6 模拟链路、完整运行验收 |

## 实际验证记录

最终测试使用 `go.mod` 指定的 Go 1.26.6。Windows 负责交叉编译，Linux VM 执行 Go 测试，避免再次运行被安全软件拦截的 Windows 测试程序。

| 检查 | 实际结果 | 范围与限制 |
| --- | --- | --- |
| Linux 目标 `go vet ./...` | 通过 | 静态检查；不证明功能满足规格 |
| 8 个 Go 包的 Linux 测试 | 42 个顶层测试通过 | api 3、cgroupcap 1、core 18、cpu 6、hostmetrics 3、incusx 8、sample 2、store 1；子测试包含多组故障注入 |
| Incus 可选集成检查 | 单独运行 1 项通过 | 已有容器内 `exit 7` 与 `cat` stdin；普通测试中的此项跳过，另行显式启用；不更改容器配置 |
| `npm test` | 10/10 通过 | 真实 Vue runtime/SSR，包括路由迟到响应、取消、凭据分批/清除、图表质量、名称路由；未进行真实浏览器视觉验收 |
| `npm run build` | 通过 | vue-tsc + Vite，更新 `internal/web/dist` |
| Linux Agent 构建 | 通过 | linux/amd64、CGO=0，产物未安装到 VM 服务位置 |
| `sh -n deploy/install.sh` | 通过 | 仅语法检查；本轮没有执行安装脚本 |
| `npm audit --json` | 0 项已知漏洞 | 当前锁文件及当时公告库；不覆盖应用自身逻辑漏洞 |
| 新 Agent 的 `govulncheck -mode=binary` | 退出 0，符号级命中 0 | 工具另报依赖模块中的 20 项公告，但未发现当前二进制调用；不能表述为全部依赖绝对安全 |
| 已安装旧 Agent 的同一漏洞扫描 | 退出 3，6 项标准库符号命中 | Go 1.26.4；旧服务未更新，详情见阶段报告 |
| 敏感文件与内容检查 | 未发现真实私钥、Token 或明文运行凭据 | 检查文件名、凭据格式和字面赋值；命中仅为测试夹具。各次提交另行检查暂存内容；不是完整泄露证明 |

候选二进制 SHA-256：`ebc43a0e586d73e6a6c9d64850219606aa83e2f6adb5a192f2758beaf28f4864`。

前后端修复均完成独立只读复核。后端复核额外发现并促成了“等待超时不能视为操作终止”的修复，相关重启故障场景已加入测试。未修改 Runtime 管理的正式结论，当前没有一个批次因本文件而自动变为正式 passed。

## 下一轮推进顺序

1. 先单独处理 F03：共享 forward、删除失败保留预留、外部冲突；用 2 台测试小鸡验证创建/删除/失败恢复，不同时展开全部 81 项。
2. F05：在已确认归属的测试实例上分别验父/子 CPU、内存、磁盘、带宽；每种资源独立记录负载与回读证据。
3. F02 与 F06：任务恢复和生命周期互相依赖，按创建、启停、删除、重装小批推进。
4. F04 分成 IPv4 出入站、IPv6-only/NAT66、前缀直路由三个实现与实测子批；先补齐必要网络环境。
5. F01/F07 补齐操作反馈、安全控制和监控缺口；F08 最后做安装升级与 8–16 台集成。高优先级的旧服务工具链/暴露面问题应先于扩大测试入口处理。

完整关键代码地图、通俗技术解释、风险和 Git 提交边界见 [阶段技术审阅](stage-review-2026-09-11.md)。
