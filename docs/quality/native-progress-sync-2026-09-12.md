# Native 说明与证据同步（2026-09-12）

> 历史记录：本文件保存 2026-09-12 范围整理及 2026-09-13 恢复时的记录，正文中的“当前”均指相应记录时点。后续候选和待办维护在[当前交接](current-handoff.md)，正式状态由 Runtime 管理。

下文保存范围整理时的文档、源码对应关系和已留存证据核对：当时准备重新划定交付范围，没有修改业务实现、连接母机或重新运行 Go/Vue/实机测试。代码核对基线为 `ea850a0`；现有测试结果均标为 2026-09-11 的历史证据。

后续确认（历史）：用户于 2026-09-12 明确确认当时 12 项交付边界、原 81 项的保留与去向、顺序推进方式；Runtime 进入 Build（当时状态版本 25，12 项均待验）。随后增加高优先部署验收 A13。当前阶段和结果始终查询 Runtime；本表的原 81 项历史结论不随范围确认改变。

原 Native 的 **17 passed / 56 failed / 8 blocked，共 81 项**属于第一轮候选，不是当前完成率。本表逐项保留该历史结果；“旧失败理由过时”只表示已有新证据，不能直接改成正式 passed。后续 Runtime 因重新 Shape 清空或重建当前验收时，这份历史记录与原需求仍保留。

原 A1–A81 的完整业务文字、来源覆盖和原工程要求见[完整首期基线](../planning/first-release/brief.md)。[交付计划](../planning/delivery-plan.md)为每项保留唯一主归属。当前 foundation 的 A1–A13 是局部交付编号，与本表原 A 编号不是同一矩阵。A13 在 brief 首位，因此 Runtime 顺序 ID A1 对应文档 A13，后续提交验收结果须按返回正文匹配。

## 2026-09-13 恢复与安装规格同步

用户请求恢复项目，并在核对代码/规格差异后明确选择「保留最新提交的行为，并同步正式规格」。当前以 `58bab0af223a76ca2ed4f41cfbfaa9aa63549c20` 为安装实现基线，更新当前 brief、三份 Spec、README、认证说明及项目知识入口。A1–A12 文字与完整首期四份基线保留；A13 新默认值、交互和 Incus 清除边界见 brief 的 D48–D52。

| 本次实际读取 | 结果 | 证明范围 |
| --- | --- | --- |
| Git 工作区与最近提交 | 开始时工作区干净，HEAD 为 58bab0a | 恢复起点；本轮只修改文档及由 Runtime 管理的流程状态 |
| [v0.1.1 Release](https://github.com/TXL-325/particeps/releases/tag/v0.1.1) | 发布于 2026-09-12T19:19:52Z，含 Agent、install.sh、SHA256SUMS 三个资产 | 已存在正式 Release，无需重新打 tag 才能开始验证 |
| [发布流水线 34713809393](https://github.com/TXL-325/particeps/actions/runs/34713809393) | headSha 为 58bab0a；日志记录 `Ran 12 tests in 0.851s` / `OK`，随后前端类型/构建和 Linux amd64 构建成功 | 复用这次 CI 记录；未重新跑测试，也不将 mock 或编译当作安装实机验收 |
| SSH 母机 192.168.143.145 | Debian 13.6 x86_64，Incus client/server 6.0.4；Agent、Incus 均 active | 当前运行状态；没有执行停止/重启连续性验证 |
| 已安装 Agent SHA256 | `565ceac26baff55bc9879f71d5c2f32eec2e2ff02c8baf31642c74227e8fb7e6`，与 Release asset digest 相同 | 母机运行文件与已发布构建匹配；不证明安装过程每步成功 |
| 选定的实际配置字段 | `listen: 0.0.0.0:8792`，`session_cookie_secure: false`，标准项目/网桥/池/数据路径 | 与新默认值相符；未读取或记录密码、Token |
| 实际网桥和池 | IPv4 `10.80.0.1/24` NAT；IPv6 `fd42:cf1e:9381:29f7::1/64` NAT；池为 lvm thin、16GiB | 这台母机的现存配置；16GiB 不是新安装的固定默认值，ULA 不证明公网 IPv6 |
| 实例与备份入口 | particeps 实例列表为空；`/var/lib/particeps/backups/latest` 不存在 | 本轮没有两实例或回滚前后证据；不能引用旧实例/旧 IPv6 地址为当前状态 |

检查使用 `gh release view`、`gh run list/view --log` 和 SSH 下的只读 systemctl/sha256sum/Incus 查询。没有部署新程序、修改配置、创建实例、停止服务、卸载 Incus 或操作浏览器。先复用有效 CI 记录，文档改动只执行文字覆盖、链接和差异检查。

后续 Build 优先处理 A13 的具体缺口，以下为源码调查线索，尚非正式 Verifier 判定：

- 首装 `do_fresh` 在下载校验之前调用包安装和 `ensure_incus_resources`；须修正或证明 SHA256/ELF 失败时仍满足不改配置/后端资源的已保留要求。
- `do_rollback` 直接复制程序和数据库，再决定是否重启；须核对运行中二进制覆盖、SQLite 活连接/WAL 以及真实回滚后的管理数据。现有 mock 用普通文件模拟程序，不证明此路径安全完成。
- 全卸在后端删除之后才停止 Agent，部分 Incus 查询错误被当作空结果；须验证没有并发创建、误判已清空或错误报告成功。
- 现有测试把 `flock` 模拟为始终成功，并跳过真实首装资源准备/管理员执行；不覆盖真实并发锁、池选择、IPv6 分支或交互 Incus 清除。完整生命周期需在可清理的实验环境验证，保留实际前后记录。

部署优先项完成后继续两种系统真实创建、Web/API、初始凭据、端口修改/冲突/停启/删除隔离，以及 Agent 真正停止期间的连接连续性。旧 F01/F03 可按影响范围提供线索，不能直接把当前 13 项标为通过。

## 主要同步结论

F03 已补充共享 IPv4 入口、追加/编辑、停启与删除清理的本地证据；凭据保护、CPU 校验和回读、监控轮询/配额曲线也已有代码修复。重装、终端、完整任务恢复、指定出口、IPv6/NAT66、完整监控、安装升级与规模验证仍保留欠项。

两项历史 passed 必须列入复验：原 A71 的勾选池约束可能被独立的 NAT 地址输入绕过；原 A77 的实例/网桥后来加入 IPv6，不能继续用该实例证明 IPv4-only。原 A18/A47 的静态限额不是负载保证，原 A30/A39 不是 SSH 登录证据，原 A53 尚未覆盖未来终端。

原 A81 后误并入的“尚未最终 Shape 确认”是文档段落结构问题，不是 CPU 验收要求。完整首期基线保留 CPU 业务全文并把编号说明放入独立标题；新验收列表后不追加普通段落。

## 证据索引

- E0：原 Native 状态版本 20，Build 修复第 2 轮；第一轮验收完成于 2026-09-11T11:32:26.559Z。[原报告及逐项原因快照](native-verification-baseline-2026-09-11.md)另行保留，原始正式文件也可从 Git `8795041:docs/comet/changes/particeps-foundation/` 读取；本次调整前实现提交为 `ea850a0`。
- E1：[F03 报告](f03-port-forwarding-2026-09-11.md)，包括正常/失败边界、暂停 Agent 的 UDP 流、停启、删除隔离及本地实测限制。
- E2：F03 [最终双实例批次](evidence/f03-2026-09-11/final-create-summary.json)、[宿主冲突](evidence/f03-2026-09-11/host-conflict-result.json)、[清理结果](evidence/f03-2026-09-11/cleanup-result.json)、[安装后结果](evidence/f03-2026-09-11/installed-result.json)、[Go 结果摘要](evidence/f03-2026-09-11/unit-results.json)。这些是摘要，不是完整原始执行日志。
- E3：[CPU 校验](../../internal/cpu/validate.go)、[资源修改](../../internal/core/instances.go)、[资源核对](../../internal/core/reconciliation.go)、[资源行为测试](../../internal/core/resources_test.go)。
- E4：[凭据封装与领取](../../internal/core/credentials.go)、[凭据测试](../../internal/core/credentials_test.go)、[任务查询](../../internal/core/instances.go)、[迁移清理](../../internal/store/store.go)。
- E5：[CPU 图表](../../web/src/components/charts/ResourceCharts.vue)、[实例轮询](../../web/src/composables/useInstanceDetail.ts)、[母机轮询](../../web/src/views/OverviewView.vue)、[采样](../../internal/core/app.go)、[Vue 行为测试](../../web/tests/behavior.test.cjs)。
- E6：[本地网络实验环境](../development/network-lab.md)、[阶段环境快照](stage-review-2026-09-11.md)、[功能审阅](functional-audit.md)。
- E7：[地址池保存](../../internal/core/app.go)、[独立 NAT 地址输入](../../web/src/components/settings/NetworkPoolSettings.vue)、[创建网卡与地址选择](../../internal/core/instances.go)。原 A71/A77 需要复核，不继承为当前通过。
- E8：[业务编排](../../internal/core/instances.go)、[Incus 后端](../../internal/incusx/client.go)、[API](../../internal/api/server.go)、[当前界面](../../web/src/views/InstanceDetailView.vue)、[安装脚本](../../deploy/install.sh)与[现有功能缺口](functional-audit.md)。

## 原 81 项的历史结果与当前证据

| 原 ID | 第一轮结果 | 本次证据分类 | 应保留的事实与限制 | 依据 |
| --- | --- | --- | --- | --- |
| A1 | passed | 保留历史结果 | 上一候选曾通过；本次仅保留历史状态，仍须按实际影响复验，不能扩展到尚未实现的路径。 | E0 |
| A2 | passed | 保留历史结果 | 上一候选曾通过；本次仅保留历史状态，仍须按实际影响复验，不能扩展到尚未实现的路径。 | E0 |
| A3 | passed | 保留历史结果 | 上一候选曾通过；本次仅保留历史状态，仍须按实际影响复验，不能扩展到尚未实现的路径。 | E0 |
| A4 | failed | 部分修复 | 端口 API/UI 已补充，认证 API 与页面送达有记录；完整 Web/API 功能仍缺，未做真实浏览器业务验收。 | E1、E2 |
| A5 | failed | 部分修复 | 端口 API/UI 已补充，认证 API 与页面送达有记录；完整 Web/API 功能仍缺，未做真实浏览器业务验收。 | E1、E2 |
| A6 | passed | 保留历史结果 | 上一候选曾通过；本次仅保留历史状态，仍须按实际影响复验，不能扩展到尚未实现的路径。 | E0 |
| A7 | passed | 保留历史结果 | 上一候选曾通过；本次仅保留历史状态，仍须按实际影响复验，不能扩展到尚未实现的路径。 | E0 |
| A8 | failed | 旧失败理由过时 | 同一批 C、D 均 ok/done；共享 .12 的双实例与 TCP/UDP 有留存证据，尚非 8–16 台验证。 | E1、E2 |
| A9 | failed | 仍未完成 | 创建之外的持久批量生命周期及逐台结果仍缺；重装当前安全拒绝，不能按功能已实现表述。 | E8 |
| A10 | failed | 仍未完成 | 创建之外的持久批量生命周期及逐台结果仍缺；重装当前安全拒绝，不能按功能已实现表述。 | E8 |
| A11 | failed | 仍未完成 | 创建之外的持久批量生命周期及逐台结果仍缺；重装当前安全拒绝，不能按功能已实现表述。 | E8 |
| A12 | failed | 仍未完成 | 创建之外的持久批量生命周期及逐台结果仍缺；重装当前安全拒绝，不能按功能已实现表述。 | E8 |
| A13 | failed | 仍未完成 | 创建之外的持久批量生命周期及逐台结果仍缺；重装当前安全拒绝，不能按功能已实现表述。 | E8 |
| A14 | failed | 仍未完成 | 创建之外的持久批量生命周期及逐台结果仍缺；重装当前安全拒绝，不能按功能已实现表述。 | E8 |
| A15 | failed | 仍未完成 | 创建之外的持久批量生命周期及逐台结果仍缺；重装当前安全拒绝，不能按功能已实现表述。 | E8 |
| A16 | passed | 保留历史结果 | 上一候选曾通过；本次仅保留历史状态，仍须按实际影响复验，不能扩展到尚未实现的路径。 | E0 |
| A17 | failed | 旧失败理由过时 | 端口自动/指定追加与目标编辑已有 API、UI 和实测；仍需真实浏览器操作复验。 | E1、E2 |
| A18 | passed | 保留历史结果 | 上一候选曾通过；本次仅保留历史状态，仍须按实际影响复验，不能扩展到尚未实现的路径。 | E0 |
| A19 | failed | 部分修复 | CPU 编号/额度校验、资源操作终态与回读已实现；资源编辑 UI、真实运行中改配与重启保持仍待完成或验证。 | E3 |
| A20 | failed | 仍未完成 | 完整网络实时/历史图、查询范围和粒度未完成；样本字段存在不构成功能通过。 | E5 |
| A21 | failed | 仍未完成 | 完整网络实时/历史图、查询范围和粒度未完成；样本字段存在不构成功能通过。 | E5 |
| A22 | failed | 部分修复 | 母机与实例 CPU 图均有 5 秒轮询和按时间坐标绘制；历史保留、范围选择、粒度和轴标仍不完整。 | E5 |
| A23 | failed | 部分修复 | 母机与实例 CPU 图均有 5 秒轮询和按时间坐标绘制；历史保留、范围选择、粒度和轴标仍不完整。 | E5 |
| A24 | failed | 仍未完成 | 进程 CPU 已如实标为累计秒，尚非相邻采样得到的实时占用。 | E5 |
| A25 | blocked | 补有环境证据 | 已有 VMware 硬件记录、Windows/母机/小鸡链路与本地 IPv4/ULA IPv6 证据；手工网络配置不等于 Agent 网络能力或完整规模验收。 | E6、E1 |
| A26 | blocked | 补有环境证据 | 已有 VMware 硬件记录、Windows/母机/小鸡链路与本地 IPv4/ULA IPv6 证据；手工网络配置不等于 Agent 网络能力或完整规模验收。 | E6、E1 |
| A27 | passed | 保留历史结果 | 上一候选曾通过；本次仅保留历史状态，仍须按实际影响复验，不能扩展到尚未实现的路径。 | E0 |
| A28 | failed | 仍未验证 | 现有最终双实例批次不能替代分别覆盖 8 台和 16 台的管理监控记录。 | E1 |
| A29 | passed | 保留历史结果 | 上一候选曾通过；本次仅保留历史状态，仍须按实际影响复验，不能扩展到尚未实现的路径。 | E0 |
| A30 | passed | 保留历史结果 | 上一候选曾通过；本次仅保留历史状态，仍须按实际影响复验，不能扩展到尚未实现的路径。 | E0 |
| A31 | failed | 旧失败理由过时 | 端口自动/指定追加与目标编辑已有 API、UI 和实测；仍需真实浏览器操作复验。 | E1、E2 |
| A32 | failed | 部分修复 | 停机保留、启动恢复及地址重绑已有证据；完整重启与重装保留组合仍须验收，重装未实现。 | E1、E8 |
| A33 | failed | 旧失败理由过时 | 清理失败保留占用；确认实例删除后事务回收。删除隔离有实测，失败保护主要为自动化故障注入。 | E1、E2 |
| A34 | failed | 部分修复 | 所选入口 DNAT 已有本地证据；管理员指定出口 SNAT 尚未实现，复合验收不能整体通过。 | E1、E8 |
| A35 | failed | 仍未完成 | 指定出口、独立地址、IPv6/NAT66、来源限制及网络能力约束按各自原要求保留，不以地址字段或手工实验代替实现。 | E6、E8 |
| A36 | passed | 保留历史结果 | 上一候选曾通过；本次仅保留历史状态，仍须按实际影响复验，不能扩展到尚未实现的路径。 | E0 |
| A37 | blocked | 仍未验证 | 已有 Alpine 运行与最终创建记录；尚未找到本候选 Debian 13 实际系统对应的完整证据。 | E1、E8 |
| A38 | blocked | 仍需实现或验证 | 密码/公钥 SSH、关闭密码认证、重置策略与 Web 终端仍有缺口；TCP 映射到 22 不是 SSH 身份登录。 | E1、E8 |
| A39 | passed | 保留历史结果 | 上一候选曾通过；本次仅保留历史状态，仍须按实际影响复验，不能扩展到尚未实现的路径。 | E0 |
| A40 | failed | 仍需实现或验证 | 密码/公钥 SSH、关闭密码认证、重置策略与 Web 终端仍有缺口；TCP 映射到 22 不是 SSH 身份登录。 | E1、E8 |
| A41 | failed | 仍需实现或验证 | 密码/公钥 SSH、关闭密码认证、重置策略与 Web 终端仍有缺口；TCP 映射到 22 不是 SSH 身份登录。 | E1、E8 |
| A42 | blocked | 补有环境证据 | 50GB 根盘和约 16GiB LVM 池已有记录；基于实际余量的日志/指标预算及空间检查仍待补齐。 | E6、E8 |
| A43 | failed | 部分实现，仍未完成 | 创建任务持久化、幂等及结果汇总已有修复；全生命周期任务执行器、失败项重试、重启与结果不明恢复仍缺。 | E8 |
| A44 | failed | 部分实现，仍未完成 | 创建任务持久化、幂等及结果汇总已有修复；全生命周期任务执行器、失败项重试、重启与结果不明恢复仍缺。 | E8 |
| A45 | failed | 部分实现，仍未完成 | 创建任务持久化、幂等及结果汇总已有修复；全生命周期任务执行器、失败项重试、重启与结果不明恢复仍缺。 | E8 |
| A46 | failed | 部分实现，仍未完成 | 创建任务持久化、幂等及结果汇总已有修复；全生命周期任务执行器、失败项重试、重启与结果不明恢复仍缺。 | E8 |
| A47 | passed | 保留历史结果 | 上一候选曾通过；本次仅保留历史状态，仍须按实际影响复验，不能扩展到尚未实现的路径。 | E0 |
| A48 | failed | 部分修复 | CPU 编号/额度校验、资源操作终态与回读已实现；资源编辑 UI、真实运行中改配与重启保持仍待完成或验证。 | E3 |
| A49 | failed | 仍未完成 | 保留各原条目的可调采样、分钟汇总/30天、完整概览、排序和周期流量要求，不能因局部采样修复标过。 | E5、E8 |
| A50 | failed | 仍未完成 | 保留各原条目的可调采样、分钟汇总/30天、完整概览、排序和周期流量要求，不能因局部采样修复标过。 | E5、E8 |
| A51 | failed | 部分修复 | NULL/质量字段和图表断点已实现；尚缺最后有效采样时间的 UI 展示，完整缺测场景待复验。 | E5 |
| A52 | failed | 旧失败理由过时 | CPU 曲线已按历史 quotaPercent 绘制，已有行为测试记录；须在新候选正式复验。 | E5 |
| A53 | passed | 保留历史结果 | 上一候选曾通过；本次仅保留历史状态，仍须按实际影响复验，不能扩展到尚未实现的路径。 | E0 |
| A54 | failed | 部分修复 | 普通任务查询不再返回明文；AES-GCM、15分钟有效、管理权限一次领取及事务清除已有。过期未领取密文的后台清理仍需补齐。 | E4 |
| A55 | failed | 仍未完成 | 端口契约已补充；覆盖全部首期操作、任务和监控的 OpenAPI 与示例仍不完整。 | E1、E8 |
| A56 | failed | 补有局部部署证据 | F03 手工更新有备份与包/运行进程哈希；通用安装升级脚本、资源归属、回滚演练仍未达到完整要求。 | E1、E2、E8 |
| A57 | blocked | 补有有限连续性证据 | 暂停候选 Agent 时两条 UDP 流继续、安装后原小鸡仍运行；尚非实际停止/重启全程业务连接存活的完整证明。 | E1、E2 |
| A58 | failed | 部分修复 | Debian 默认模板和资源修改终态/回读已有修复；编辑 UI、资源不足及实际资源效果仍须补齐或验收。 | E3、E8 |
| A59 | blocked | 仍未验证 | 已分发普通/强制电源动作；F03 停启测试不覆盖优雅停止与强制停止的完整区别。 | E1、E8 |
| A60 | failed | 仍需实现或验证 | 密码/公钥 SSH、关闭密码认证、重置策略与 Web 终端仍有缺口；TCP 映射到 22 不是 SSH 身份登录。 | E1、E8 |
| A61 | failed | 部分修复 | Debian 默认模板和资源修改终态/回读已有修复；编辑 UI、资源不足及实际资源效果仍须补齐或验收。 | E3、E8 |
| A62 | failed | 部分修复 | Debian 默认模板和资源修改终态/回读已有修复；编辑 UI、资源不足及实际资源效果仍须补齐或验收。 | E3、E8 |
| A63 | failed | 部分修复 | Debian 默认模板和资源修改终态/回读已有修复；编辑 UI、资源不足及实际资源效果仍须补齐或验收。 | E3、E8 |
| A64 | failed | 仍未完成 | 保留各原条目的可调采样、分钟汇总/30天、完整概览、排序和周期流量要求，不能因局部采样修复标过。 | E5、E8 |
| A65 | failed | 仍未完成 | 保留各原条目的可调采样、分钟汇总/30天、完整概览、排序和周期流量要求，不能因局部采样修复标过。 | E5、E8 |
| A66 | failed | 仍未完成 | 期望电源与 boot.autostart 协调、真实本机镜像登记及创建/重装衔接仍需完成。 | E8 |
| A67 | failed | 仍未完成 | 期望电源与 boot.autostart 协调、真实本机镜像登记及创建/重装衔接仍需完成。 | E8 |
| A68 | failed | 仍未完成 | 保留各原条目的可调采样、分钟汇总/30天、完整概览、排序和周期流量要求，不能因局部采样修复标过。 | E5、E8 |
| A69 | failed | 仍未完成 | 指定出口、独立地址、IPv6/NAT66、来源限制及网络能力约束按各自原要求保留，不以地址字段或手工实验代替实现。 | E6、E8 |
| A70 | passed | 保留历史结果 | 上一候选曾通过；本次仅保留历史状态，仍须按实际影响复验，不能扩展到尚未实现的路径。 | E0 |
| A71 | passed | 历史通过需重审 | natIPv4 可独立填写，未见必须属于勾选 IPv4 集合的校验；不能继续沿用“未勾选绝不用”的旧结论。 | E7 |
| A72 | failed | 仍未完成 | 指定出口、独立地址、IPv6/NAT66、来源限制及网络能力约束按各自原要求保留，不以地址字段或手工实验代替实现。 | E6、E8 |
| A73 | failed | 旧失败理由过时 | 所选 .11/.12 入口共享 DNAT 与正确目标已有本地实测；不扩大为真实公网或指定出口 SNAT 已通过。 | E1、E2 |
| A74 | failed | 仍未完成 | 指定出口、独立地址、IPv6/NAT66、来源限制及网络能力约束按各自原要求保留，不以地址字段或手工实验代替实现。 | E6、E8 |
| A75 | failed | 仍未完成 | 指定出口、独立地址、IPv6/NAT66、来源限制及网络能力约束按各自原要求保留，不以地址字段或手工实验代替实现。 | E6、E8 |
| A76 | failed | 仍未完成 | 指定出口、独立地址、IPv6/NAT66、来源限制及网络能力约束按各自原要求保留，不以地址字段或手工实验代替实现。 | E6、E8 |
| A77 | passed | 历史通过需重审 | 原小鸡和共享网桥随后增加 IPv6/SLAAC；创建 v4 模式未显式禁止 IPv6，旧实例证据不能证明 IPv4-only。 | E6、E7 |
| A78 | failed | 仍未完成 | 指定出口、独立地址、IPv6/NAT66、来源限制及网络能力约束按各自原要求保留，不以地址字段或手工实验代替实现。 | E6、E8 |
| A79 | failed | 仍未完成 | 指定出口、独立地址、IPv6/NAT66、来源限制及网络能力约束按各自原要求保留，不以地址字段或手工实验代替实现。 | E6、E8 |
| A80 | failed | 仍未完成 | 指定出口、独立地址、IPv6/NAT66、来源限制及网络能力约束按各自原要求保留，不以地址字段或手工实验代替实现。 | E6、E8 |
| A81 | blocked | 仍需负载验收 | 父/子 cpu.max 与校验/恢复已有；无多实例负载下合计 CPU 测量。旧条目误并入 Shape 阶段说明，业务全文以首期基线 A81 为准。 | E3、E6 |

## 接续规则

- 原首期要求保持有效；迁入后续交付不等于通过、取消或删除。
- 当前交付的候选必须绑定实际代码/二进制与检查结果；历史记录仅作为可追溯证据，受后续修改影响的路径重新验证。
- Go 79 项、Vue 16 项是 F03 开发检查记录，不能替代原 81 项或当前 13 项语义验收。
- 只对本次文档执行覆盖、链接和格式核对；正式验收仍由 Runtime 执行检查并接受新的独立 Verifier 结果。

## 范围整理时的核对与 Runtime 结果（历史）

范围整理时已核对原 81 条验收与 Git 基线逐条文字一致，交付映射 81 项各有唯一主归属，历史结果仍为 17/56/8，当时草案为连续的 A1–A12。本地 Markdown 链接与 `git diff --check` 通过；两次独立文档审阅提出的来源映射、旧阶段文字和实际退出路径表述问题已修正。这些是文档检查，不是 Go/Vue 或实机功能验收。

当时 Runtime 通过公开 `comet native next` 将 change 返回 Shape，并登记完整草案为等待最终确认，12 项均待验。旧活动报告由 Runtime 在返回 Shape 时移除，另存的历史快照保留其原结论；没有手工修改 Runtime 状态或伪造新的通过数。此等待已由上方的用户确认与 Build 推进接续。

## 确认记录与项目知识接入

确认后同步了 brief 的 Decisions、三份完整 Spec、交付计划与入口说明，验收正文没有变化。当前 A1–A12 验收段和四份完整首期基线的 SHA-256 与本次会话修改前一致。

[项目知识入口](../knowledge/project-context.md)保存已确认范围、架构、验证方式和正式来源。[Comet 配置](../../.comet/config.yaml)通过 `knowledge.local.include` 补充当前 brief/Spec、交付计划、进度同步、README 和相关实验记录；默认只发现正式 Spec 和归档，原先无法检索这些新交付依据。

实际执行 `comet knowledge rebuild . --json`，无诊断错误；分别查询当前范围、S0–S7/原 81 项归属、F03 历史证据及 Go/Vue 构建方式，已命中对应文档及内容。构建阶段的 `comet task` 也已返回新范围和交付计划的上下文条目。17 份 Markdown 的 133 个本地链接有效，原 81 项映射无遗漏重复，`git diff --check` 通过。知识的自动结构记录仍为 3 条；额外文档通过正文检索提供，不把文档召回计作新增结构记录或验收通过。

本次只同步确认、文档和知识检索，没有提交 Builder 候选、执行 Go/Vue 或实机功能测试；当前 12 项仍待后续 Build/Verify 逐项完成。
