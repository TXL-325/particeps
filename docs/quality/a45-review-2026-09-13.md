# A4/A5 提交前审查与修复

日期：2026-09-13。基线为 `49e6a489d60adf483f347fe68d7c39c934017e6e`。用户要求先由子 agent review，自主修复，再由子 agent 整理文档，最后由主 agent 审查并 commit。本次提交包含此前 A4 的 HTTP 创建修复、A5 的端口预留与 SSH 初始化，以及本轮发现的两项 P2 修复。

[A4 创建记录](a4-creation-2026-09-13.md)和[A5 端口与 SSH 记录](a5-ports-ssh-2026-09-13.md)保留当时的实机结果及候选摘要；本页记录后续审查，不把历史部署视为新候选的实机验证。本次 44 个源码、生成资源及构建输入摘要、二进制和日志摘要见[结构化证据](evidence/a45-2026-09-13/verification.json)。完整 foundation 的阶段和正式验收结果仍由 Runtime 管理。

## 独立审查与修复

三位独立 Reviewer 分别为 `/root/a45_review_core`、`/root/a45_review_ssh`、`/root/a45_review_web`，覆盖后端端口/API/存储、SSH/exec 和前端创建/映射路径。修复后的复核均为 passed，两项 P2 关闭，未发现新的阻断问题。

| 发现 | 影响与修复 | 本轮复核 |
| --- | --- | --- |
| P2：`waiting-address` 时 Web 无法解除映射 | API 已支持无地址时禁用，但页面统一锁住全部操作。现允许在该状态解除已设为启用的映射；新增、启用和目标编辑仍锁定。忙碌、实例操作中、未确认查询和待核对状态继续阻止操作。 | 定向端口行为检查 11/11 通过，原前端 Reviewer 复核通过 |
| P2：`PrepareSSH` / `StartSSH` 的 exec 结果未确认时缺少持久保护 | SSH 命令可能仍在执行，普通失败路径却可能允许后续修改或删除。现执行前持久记录 `uncertain`；确认终态后才清除，未知结果保留阶段和不含查询参数的 operation path，阻止转发、端口及生命周期操作绕过保护。 | core、incusx 受影响包检查通过，原 core 和 SSH Reviewer 复核通过 |

SSH 修复由 `/root/a45_ssh_fix` 实现，覆盖进程/响应丢失、已知终态、持久记录失败及数据库重开后的保护。这里的数据库重开回归不等同于实际停止 Agent 的 A11 连续性验收。

## 候选与检查

本次候选的前端资源为 `index-CWN5eAUr.js`、`index-KVRaOcLY.css`。Linux amd64 Agent 构建成功，文件保存在 `dist/a45-review-20260913/particeps-agent`，SHA256：

```text
db04ab7f0192f2b0fa4df996161643b3cb03f80d6bfacb3e5f609bf9558673d7
```

| 检查 | 实际结果与证明范围 |
| --- | --- |
| Web 端口定向检查 | 11/11 通过；覆盖等待地址时解除映射及 busy、未知状态、实例操作等限制 |
| 最终 `npm run build` | `vue-tsc --noEmit` 与 Vite 构建通过 |
| Linux core 测试二进制 | 83 项顶层测试通过，计入子测试为 120 项；无失败、无跳过 |
| Linux incusx 测试二进制 | 27 项顶层测试通过，计入子测试为 96 项；另跳过 `TestIncusReadOnlyIntegration`，未设置专用 socket/guest；无失败 |
| Linux amd64 Agent 构建 | 成功；候选摘要见上文及结构化证据 |
| 修复后独立复核 | core、SSH、Web 三位 Reviewer 均 passed |

core/incusx 在隔离目录 `/tmp/particeps-a45-review-zs9ecji7/` 运行交叉编译的测试二进制，参数为 `-test.v -test.timeout 120s`；仅使用临时库、模拟后端及 sshd 配置解析，没有操作用户实例。日志下载到本机 `dist/a45-review-20260913/`，与远端 SHA256 一致；核对目录成员后已删除远端临时目录。本地日志保留，其摘要进入结构化证据。未因这两项修复机械重跑安装脚本或所有历史检查。

本轮新增修复仅在隔离测试目录和模拟后端验证，没有部署到实验母机，也没有创建、删除或修改用户实例。A4/A5 已有浏览器、Windows SSH 与 TCP/UDP 实测仍只证明对应历史候选；本轮组件行为检查不冒称新的真实浏览器或实机验证。

## 只读实机核对与接续

主 agent 本轮及测试目录清理后只读确认 `particeps-agent`、`incus`、`particeps-vmnet2-forwarding` 均 active。运行二进制仍为 A5 部署版本：

```text
4ba29c0d59e8850b79baa6aff8875e8071bd461c13b41632109c6332f74ef375
```

`particeps` 项目现有四台 Running 实例：`p-befae687800f`、`p-e7ade30abd02`、`p-232ca9b0caac`、`p-6d04bc1cdf9e`。后两台是用户后续资源，不是 A5 测试残留；A5 清理时“仅保留两台”和“40 条旧规则”均为当时快照，不能作为当前清理清单。

guest 历史上的 `needs-reconciliation`、`uncertain=1` 电源结果仍是 A8 的调查线索；本轮未重新查询该数据库状态，也未解除保护。母机备份 `/root/particeps-backups/a5-20260913-153837/` 早于后续用户资源，不能盲目恢复其数据库。

`InstallRootKey` / `FilePush` 的既有未确认写入边界保留为后续调查线索，本轮没有修复该路径；不能把 `PrepareSSH` / `StartSSH` 的结果保护扩展为全部凭据写入路径均已完成验证。

Runtime 已通过公开命令对齐此前用户确认的端口语义并回到 Build，未执行全量验收或归档。下一步仍为 A6/A7 剩余的共享入口、冲突/并发分配、自动/指定追加及编辑隔离，再推进 A8/A9 生命周期与删除、A11 Agent 连续性。本次审查和局部修复不表示这些条目、完整 S3、公网/IPv6 或整个 foundation 已通过。没有 push、发布 Release 或部署本轮新候选。
