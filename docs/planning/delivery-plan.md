# 首期交付计划

本页维护原首期验收的唯一主归属、依赖和局部覆盖关系。[完整首期 brief](first-release/brief.md)及其三份 Spec 保存原始来源与完整承诺；[当前 brief](../comet/changes/particeps-foundation/brief.md)定义本次交付，候选和证据从[当前交接](../quality/current-handoff.md)进入。

“原 A…”指首期基线，“foundation/A…”指本次局部编号。范围收敛允许基础能力独立交付，后续资源、任务、终端、网络和监控承诺继续保留；局部通过不转换为原 81 项通过。

2026-09-13 A5 的用户修订及相关映射编辑、创建 SSH 修复已完成当轮实现、独立 review、浏览器/实机验证和清理，见[A5 验证记录](../quality/a5-ports-ssh-2026-09-13.md)。默认预留 20 个连续号码，默认仅 TCP、仅首个 TCP 到 22 自动启用；UDP 在创建时可选且默认仅预留，Web 展示可用号段，旧实例已有规则保留。

用户随后要求 review、自主修复、文档整理和 commit。本次[A4/A5 提交前审查](../quality/a45-review-2026-09-13.md)补齐等待地址时的 Web 解除映射，以及 SSH exec 结果未知时的持久保护；具体候选与检查以该记录为准，新修复未部署。后续仍补齐 A6/A7 剩余共享入口、冲突、自动/指定追加及编辑隔离场景；局部结果不代表这些条目、整个 foundation 或完整 S3 通过。

当前 brief 的 N20/N21、D62/D63 覆盖原 A16/A17/A29–A31 中冲突的默认分配/自动映射语义，并以 superseded 关系保留来源。原 ID、主归属和历史首期正文不改；最终集成按这些 ID 及有效的后续决定验收，不能同时要求旧全量映射与新按需映射。

## 后续安排与原验收唯一主归属

以下 S0–S7 是路线图领域和责任归属，不是 8 个已创建的 Native change，也不是 Supervisor/Child。只有准备实现某个独立结果时才建立对应 change；若该领域仍包含多个可独立交付结果，启动前继续细分，并保留本表的原 ID 映射。

| 阶段 | 交付领域 | 原验收主归属 | 数量 | 依赖与边界 |
| --- | --- | --- | ---: | --- |
| S0 | 当前 foundation：实验母机基础管理与 IPv4 端口 | A1、A2、A6、A8、A16、A17、A27、A29、A30、A31、A33、A36、A37 | 13 | 已准备的本地母机；工程与归属、两种初始镜像、正常批量创建、端口分配/编辑/清理；当前操作必须有认证、凭据和 UI/API 门槛。 |
| S1 | 资源上限 | A18、A19、A47、A48、A58、A61、A62、A63、A81 | 9 | S0；先验父/子 CPU 和运行中改配，再分别验内存、磁盘、带宽；以实际限制和负载为证据。 |
| S2 | 持久任务与完整生命周期 | A9、A10、A11、A12、A13、A14、A15、A32、A43、A44、A45、A46、A59、A66、A67 | 15 | S0、S1；按创建恢复、启停/删除、重装依次收口；包括失败项重试、幂等、未知结果、期望电源和本机镜像。 |
| S3 | SSH、凭据与 Web 终端 | A38、A39、A40、A41、A53、A54、A60 | 7 | S0、S2；完整 SSH 公钥/密码/仅公钥策略矩阵、密码重置、重装后的凭据与不依赖 SSH 的交互终端。S0 当前创建所需 SSH 初始化及实际登录修复不代表 S3 完成。 |
| S4 | IPv4 出入站与独立地址 | A34、A72、A73、A74、A77 | 5 | S0；指定出口 SNAT、入口 DNAT、独立 IPv4 与严格 IPv4-only；复用 F03，不能只以入站证据验收出站。 |
| S5 | IPv6、网络能力组合与来源限制 | A35、A69、A70、A71、A75、A76、A78、A79、A80 | 9 | S4、S2；实施前再细化为前缀直路由、NAT66、模式与池约束、来源限制等独立增量，逐个创建 change。 |
| S6 | 监控与进程诊断 | A7、A20、A21、A22、A23、A24、A49、A50、A51、A52、A64、A65、A68 | 13 | S0、S1；与 S2 核对生命周期；资源趋势/历史、分钟汇总、周期流量、完整概览/排序及实例内部实时进程；按独立结果继续切分。 |
| S7 | 首期部署与完整集成 | A3、A4、A5、A25、A26、A28、A42、A55、A56、A57 | 10 | S0–S6；通用安装升级/回滚、完整 API 与 Web、网络与 8/16 台集成；最终复核全部原 81 项。 |

合计 81 项，每个原 ID 只有一个主归属，无遗漏、无重复。优先顺序为 S0 → S1 → S2 → S3；S4 可在 S0 之后按需要插入，S5 遵守其网络和生命周期依赖，S6 可在资源与实例契约稳定后推进。最后 S7 全量复核原 81 项及完整规格，表中 10 项只是其主要实现责任。

原 A56 的主归属仍在 S7。foundation/A13 提前交付 GitHub Release 安装/升级/回滚/卸载切片，不取消 S7 的 8–16 台集成、剩余空间预算和全量复核。不新增自动升级守护进程。

部署默认值、交互及 Incus 清除边界由[项目规格](../comet/changes/particeps-foundation/specs/project-foundation/spec.md#部署与发布)维护。脚本网桥准备不使 S5 的实例地址池、模式、路由和公网验收提前完成。

S1 内分别记录 CPU、内存、盘与带宽证据；S2 按可恢复动作递进；S5 明确拆为直路由、NAT66、能力约束/组合和来源限制等增量后再启动。不要把整张路线图再次装进一个新的巨大 change。

## 当前 13 项门槛与原要求的关系

| 当前门槛 | 交付结果 | 原要求来源或部分覆盖 |
| --- | --- | --- |
| foundation/A1 | 工程与实例归属 | 原 A1、A2、A6、A27；原 A3 的本次交付部分 |
| foundation/A2 | 当前操作的认证与授权 | 原 A53 的当前接口部分 |
| foundation/A3 | 当前创建流程的凭据保护 | 原 A39/A54 的当前创建部分 |
| foundation/A4 | 正常创建、镜像对应及必要 SSH 初始化 | 原 A6、A8、A36、A37；原 A58 的模板配置部分；用户 N22 当前创建 SSH 补充 |
| foundation/A5 | 连续号码预留、默认 TCP/可选 UDP、仅首个 TCP 到 22 默认启用、前端号段及 Windows SSH | 原 A16、A29、A30；冲突默认行为由用户 N20/D62 替代，登录要求见 N22/D64 |
| foundation/A6 | 共享入口与冲突 | 原 A16/A29 的冲突边界；原 A73 的本地入站部分 |
| foundation/A7 | 追加仅预留、按协议启停映射和目标编辑 | 原 A17、A31；追加即映射语义由 N21/D63 替代，禁用不释放归属 |
| foundation/A8 | 停止、启动与映射恢复 | 原 A32 的停止/启动与地址关联部分；原 A9/A10 的单台基础动作 |
| foundation/A9 | 删除与清理失败保护 | 原 A33；原 A14 的单台基础动作 |
| foundation/A10 | Web 与 API 操作闭环 | 原 A4/A5/A55 的本次操作部分 |
| foundation/A11 | Agent 停止与重启 | 原 A57 的当前 IPv4 管理部分 |
| foundation/A12 | 可追溯的实验交付 | 原 A25/A26/A42 的本次实验记录部分 |
| foundation/A13 | GitHub Release 安装、升级、状态、回滚与卸载及已确认的安装行为 | 原 A56 的安装/更新/备份切片；全卸及独立 Incus 清除选择为本次脚本决定，不改写首期基线 |

“部分覆盖”是证据贡献，不是原条目完成。特别保留以下边界：

- 原 A3/A4/A5/A55 仍要求完整首期能力通过 Agent、Web/API 交付；foundation/A1/A10 不改变它们的“完整范围”。
- 原 A32 的重启、重装与全部分配保持要求仍归 S2；foundation/A8 只覆盖停止、启动和地址关联，不能删掉重装文字。
- 原 A34 同时要求指定 SNAT 与 DNAT，原 A73 保留完整入口语义；本地 DNAT 证据不能替代指定出口或真实公网结论。
- 原 A43–A46 继续覆盖全生命周期的持久化、失败重试、幂等和重启恢复；正常创建与查询不让这些条目自动完成。
- 原 A53/A54 在 S3 收口完整访问路径；foundation/A2/A3 仍强制保护当前接口和创建凭据。
- 原 A38–A41 的完整主归属保持 S3；本轮 A4/A5 必须修复创建所需 SSH 服务、请求的登录策略和 Windows 实际认证，不能以 S3 后置为由留下不可用的默认 SSH 入口。
- 原 A25/A26/A28/A42/A56/A57 的完整环境、规模和集成要求留在 S7；foundation/A13 不能把 8–16 台或剩余空间预算标过。
- 原 A71/A77 的历史 passed 不直接继承，地址池与 IPv4-only 约束在对应网络交付中重新验证。

## 执行边界

此表维护交付归属和依赖；正式需求与验收以当前 brief/Spec 为准，阶段和确认边界由 Runtime 管理。当前普通 change 顺序推进，不因路线图建立 children.yaml、额外 worktree 或未来 change，也不自动 merge、push 或归档。具体待办与检查证据见[当前交接](../quality/current-handoff.md)。
